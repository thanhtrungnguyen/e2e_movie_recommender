[tool.poetry]
name = "spark-jobs"
version = "0.1.0"
description = "Spark jobs for training and streaming"
authors = ["You <you@example.com>"]

[tool.poetry.dependencies]
python = "^3.10"

[tool.poetry.group.spark.dependencies]
# Inherit from root

[tool.poetry.group.dev.dependencies]
# Inherit from root
